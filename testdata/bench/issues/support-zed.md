Support the Zed editor as an agent target

Zed has MCP support now. It would be great if `ohmylaya install` could also register the server in Zed's settings.json, like it does for Codex and OpenCode.
