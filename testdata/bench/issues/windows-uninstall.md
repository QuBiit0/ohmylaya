Uninstall leaves the bin folder behind on Windows

After `ohmylaya uninstall` the folder %USERPROFILE%\.ohmylaya\bin still exists with laya-cli.exe inside. Running uninstall again says nothing to remove.
